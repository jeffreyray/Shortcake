/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.16 from the
 * contents of GtkAccelMap.xs. Do not edit this file, edit GtkAccelMap.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "xs\\\\GtkAccelMap.xs"
/*
 * Copyright (c) 2003-2005 by the gtk2-perl team (see the file AUTHORS)
 *
 * Licensed under the LGPL, see LICENSE file for more information.
 *
 * $Id$
 */

#include "gtk2perl.h"

static void
gtk2perl_gtk_accel_map_foreach (GPerlCallback *callback, 
				const gchar *accel_path, guint accel_key, 
				GdkModifierType accel_mods, gboolean changed)
{
	gperl_callback_invoke (callback, NULL, accel_path, accel_key, 
			       accel_mods, changed);
}

#line 30 "xs\\GtkAccelMap.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 172 "xs\\GtkAccelMap.c"

XS_EUPXS(XS_Gtk2__AccelMap_add_entry); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_add_entry)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "class, accel_path, accel_key, accel_mods");
    {
	const gchar *	accel_path;
	guint	accel_key = (guint)SvUV(ST(2))
;
	GdkModifierType	accel_mods = SvGdkModifierType (ST(3))
;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	accel_path = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_add_entry(accel_path, accel_key, accel_mods);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_lookup_entry); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_lookup_entry)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, accel_path");
    PERL_UNUSED_VAR(ax); /* -Wall */
    SP -= items;
    {
	const gchar *	accel_path;
#line 57 "xs\\\\GtkAccelMap.xs"
	GtkAccelKey key;
#line 210 "xs\\GtkAccelMap.c"

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	accel_path = (const gchar *)SvPV_nolen (ST(1))
;
#line 59 "xs\\\\GtkAccelMap.xs"
	if (gtk_accel_map_lookup_entry (accel_path, &key))
	{
		EXTEND (SP, 3);
		PUSHs (sv_2mortal (newSViv (key.accel_key)));
		PUSHs (sv_2mortal (newSVGdkModifierType (key.accel_mods)));
		PUSHs (sv_2mortal (newSViv (key.accel_flags)));
	}
	else
		XSRETURN_EMPTY;
#line 226 "xs\\GtkAccelMap.c"
	PUTBACK;
	return;
    }
}


XS_EUPXS(XS_Gtk2__AccelMap_change_entry); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_change_entry)
{
    dVAR; dXSARGS;
    if (items != 5)
       croak_xs_usage(cv,  "class, accel_path, accel_key, accel_mods, replace");
    {
	const gchar *	accel_path;
	guint	accel_key = (guint)SvUV(ST(2))
;
	GdkModifierType	accel_mods = SvGdkModifierType (ST(3))
;
	gboolean	replace = (bool)SvTRUE(ST(4))
;
	gboolean	RETVAL;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	accel_path = (const gchar *)SvPV_nolen (ST(1))
;

	RETVAL = gtk_accel_map_change_entry(accel_path, accel_key, accel_mods, replace);
	ST(0) = boolSV(RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__AccelMap_load); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_load)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, file_name");
    {
	const gchar *	file_name;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	file_name = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_load(file_name);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_save); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_save)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, file_name");
    {
	const gchar *	file_name;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	file_name = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_save(file_name);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_load_fd); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_load_fd)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, fd");
    {
	gint	fd = (gint)SvIV(ST(1))
;

	gtk_accel_map_load_fd(fd);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_save_fd); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_save_fd)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, fd");
    {
	gint	fd = (gint)SvIV(ST(1))
;

	gtk_accel_map_save_fd(fd);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_add_filter); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_add_filter)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, filter_pattern");
    {
	const gchar *	filter_pattern;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	filter_pattern = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_add_filter(filter_pattern);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_foreach); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_foreach)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "class, data, foreach_func");
    {
	SV *	data = ST(1)
;
	SV *	foreach_func = ST(2)
;
#line 133 "xs\\\\GtkAccelMap.xs"
	GPerlCallback * callback = NULL;
	GType types[4];
#line 368 "xs\\GtkAccelMap.c"
#line 136 "xs\\\\GtkAccelMap.xs"
    	types[0] = G_TYPE_STRING;
    	types[1] = G_TYPE_UINT;
    	types[2] = GDK_TYPE_MODIFIER_TYPE;
    	types[3] = G_TYPE_BOOLEAN;
	callback = gperl_callback_new (foreach_func, data, 4, types, 
				       G_TYPE_NONE);
	gtk_accel_map_foreach 
		(callback, (GtkAccelMapForeach)gtk2perl_gtk_accel_map_foreach);
	gperl_callback_destroy (callback);
#line 379 "xs\\GtkAccelMap.c"
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_foreach_unfiltered); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_foreach_unfiltered)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "class, data, foreach_func");
    {
	SV *	data = ST(1)
;
	SV *	foreach_func = ST(2)
;
#line 152 "xs\\\\GtkAccelMap.xs"
	GPerlCallback * callback = NULL;
	GType types[4];
#line 399 "xs\\GtkAccelMap.c"
#line 155 "xs\\\\GtkAccelMap.xs"
    	types[0] = G_TYPE_STRING;
    	types[1] = G_TYPE_UINT;
    	types[2] = GDK_TYPE_MODIFIER_TYPE;
    	types[3] = G_TYPE_BOOLEAN;
	callback = gperl_callback_new (foreach_func, data, 4, types, 
				       G_TYPE_NONE);
	gtk_accel_map_foreach_unfiltered
		(callback, (GtkAccelMapForeach)gtk2perl_gtk_accel_map_foreach);
	gperl_callback_destroy (callback);
#line 410 "xs\\GtkAccelMap.c"
    }
    XSRETURN_EMPTY;
}

#if GTK_CHECK_VERSION (2, 4, 0)
#define XSubPPtmpAAAA 1


XS_EUPXS(XS_Gtk2__AccelMap_get); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_get)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "class");
    {
	GtkAccelMap *	RETVAL;

	RETVAL = gtk_accel_map_get(/* void */);
	ST(0) = newSVGtkAccelMap (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__AccelMap_lock_path); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_lock_path)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, accel_path");
    {
	const gchar *	accel_path;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	accel_path = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_lock_path(accel_path);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__AccelMap_unlock_path); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__AccelMap_unlock_path)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, accel_path");
    {
	const gchar *	accel_path;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	accel_path = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_accel_map_unlock_path(accel_path);
    }
    XSRETURN_EMPTY;
}

#endif
#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Gtk2__AccelMap); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Gtk2__AccelMap)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        newXS("Gtk2::AccelMap::add_entry", XS_Gtk2__AccelMap_add_entry, file);
        newXS("Gtk2::AccelMap::lookup_entry", XS_Gtk2__AccelMap_lookup_entry, file);
        newXS("Gtk2::AccelMap::change_entry", XS_Gtk2__AccelMap_change_entry, file);
        newXS("Gtk2::AccelMap::load", XS_Gtk2__AccelMap_load, file);
        newXS("Gtk2::AccelMap::save", XS_Gtk2__AccelMap_save, file);
        newXS("Gtk2::AccelMap::load_fd", XS_Gtk2__AccelMap_load_fd, file);
        newXS("Gtk2::AccelMap::save_fd", XS_Gtk2__AccelMap_save_fd, file);
        newXS("Gtk2::AccelMap::add_filter", XS_Gtk2__AccelMap_add_filter, file);
        newXS("Gtk2::AccelMap::foreach", XS_Gtk2__AccelMap_foreach, file);
        newXS("Gtk2::AccelMap::foreach_unfiltered", XS_Gtk2__AccelMap_foreach_unfiltered, file);
#if XSubPPtmpAAAA
        newXS("Gtk2::AccelMap::get", XS_Gtk2__AccelMap_get, file);
        newXS("Gtk2::AccelMap::lock_path", XS_Gtk2__AccelMap_lock_path, file);
        newXS("Gtk2::AccelMap::unlock_path", XS_Gtk2__AccelMap_unlock_path, file);
#endif

    /* Initialisation Section */

#if XSubPPtmpAAAA
#endif
#line 516 "xs\\GtkAccelMap.c"

    /* End of Initialisation Section */

#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

