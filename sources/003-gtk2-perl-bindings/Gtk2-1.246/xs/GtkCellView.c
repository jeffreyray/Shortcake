/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.16 from the
 * contents of GtkCellView.xs. Do not edit this file, edit GtkCellView.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "xs\\\\GtkCellView.xs"
/*
 * Copyright (c) 2004 by the gtk2-perl team (see the file AUTHORS)
 *
 * Licensed under the LGPL, see LICENSE file for more information.
 *
 * $Id$
 */

#include "gtk2perl.h"

#line 21 "xs\\GtkCellView.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 163 "xs\\GtkCellView.c"

XS_EUPXS(XS_Gtk2__CellView_new); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_new)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "class");
    {
	GtkWidget *	RETVAL;

	RETVAL = gtk_cell_view_new(/* void */);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_new_with_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_new_with_text)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, text");
    {
	const gchar *	text;
	GtkWidget *	RETVAL;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	text = (const gchar *)SvPV_nolen (ST(1))
;

	RETVAL = gtk_cell_view_new_with_text(text);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_new_with_markup); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_new_with_markup)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, markup");
    {
	const gchar *	markup;
	GtkWidget *	RETVAL;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	markup = (const gchar *)SvPV_nolen (ST(1))
;

	RETVAL = gtk_cell_view_new_with_markup(markup);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_new_with_pixbuf); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_new_with_pixbuf)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, pixbuf");
    {
	GdkPixbuf *	pixbuf = SvGdkPixbuf (ST(1))
;
	GtkWidget *	RETVAL;

	RETVAL = gtk_cell_view_new_with_pixbuf(pixbuf);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_set_model); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_set_model)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cell_view, model");
    {
	GtkCellView *	cell_view = SvGtkCellView (ST(0))
;
	GtkTreeModel_ornull *	model = SvGtkTreeModel_ornull (ST(1))
;

	gtk_cell_view_set_model(cell_view, model);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__CellView_set_displayed_row); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_set_displayed_row)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cell_view, path");
    {
	GtkCellView *	cell_view = SvGtkCellView (ST(0))
;
	GtkTreePath *	path = SvGtkTreePath (ST(1))
;

	gtk_cell_view_set_displayed_row(cell_view, path);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__CellView_get_displayed_row); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_get_displayed_row)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "cell_view");
    {
	GtkTreePath_own *	RETVAL;
	GtkCellView *	cell_view = SvGtkCellView (ST(0))
;

	RETVAL = gtk_cell_view_get_displayed_row(cell_view);
	ST(0) = newSVGtkTreePath_own (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_get_size_of_row); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_get_size_of_row)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cell_view, path");
    {
#line 42 "xs\\\\GtkCellView.xs"
	GtkRequisition requisition;
#line 311 "xs\\GtkCellView.c"
	GtkRequisition_copy *	RETVAL;
	GtkCellView *	cell_view = SvGtkCellView (ST(0))
;
	GtkTreePath *	path = SvGtkTreePath (ST(1))
;
#line 44 "xs\\\\GtkCellView.xs"
	gtk_cell_view_get_size_of_row (cell_view, path, &requisition);
	RETVAL = &requisition;
#line 320 "xs\\GtkCellView.c"
	ST(0) = newSVGtkRequisition_copy (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__CellView_set_background_color); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_set_background_color)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cell_view, color");
    {
	GtkCellView *	cell_view = SvGtkCellView (ST(0))
;
	const GdkColor *	color = SvGdkColor (ST(1))
;

	gtk_cell_view_set_background_color(cell_view, color);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__CellView_get_cell_renderers); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_get_cell_renderers)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "cellview");
    PERL_UNUSED_VAR(ax); /* -Wall */
    SP -= items;
    {
#line 55 "xs\\\\GtkCellView.xs"
	GList * list;
#line 357 "xs\\GtkCellView.c"
	GtkCellView *	cellview = SvGtkCellView (ST(0))
;
#line 57 "xs\\\\GtkCellView.xs"
	list = gtk_cell_view_get_cell_renderers (cellview);
	if (list)
	{
		GList * curr;

		for (curr = list; curr; curr = g_list_next (curr))
			XPUSHs (sv_2mortal (newSVGtkCellRenderer (curr->data)));

		g_list_free (list);
	}
	else
		XSRETURN_EMPTY;
#line 373 "xs\\GtkCellView.c"
	PUTBACK;
	return;
    }
}

#if GTK_CHECK_VERSION (2, 16, 0)
#define XSubPPtmpAAAA 1


XS_EUPXS(XS_Gtk2__CellView_get_model); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__CellView_get_model)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "cellview");
    {
	GtkTreeModel_ornull *	RETVAL;
	GtkCellView *	cellview = SvGtkCellView (ST(0))
;

	RETVAL = gtk_cell_view_get_model(cellview);
	ST(0) = newSVGtkTreeModel_ornull (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}

#endif /* 2.16 */
#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Gtk2__CellView); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Gtk2__CellView)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        newXS("Gtk2::CellView::new", XS_Gtk2__CellView_new, file);
        newXS("Gtk2::CellView::new_with_text", XS_Gtk2__CellView_new_with_text, file);
        newXS("Gtk2::CellView::new_with_markup", XS_Gtk2__CellView_new_with_markup, file);
        newXS("Gtk2::CellView::new_with_pixbuf", XS_Gtk2__CellView_new_with_pixbuf, file);
        newXS("Gtk2::CellView::set_model", XS_Gtk2__CellView_set_model, file);
        newXS("Gtk2::CellView::set_displayed_row", XS_Gtk2__CellView_set_displayed_row, file);
        newXS("Gtk2::CellView::get_displayed_row", XS_Gtk2__CellView_get_displayed_row, file);
        newXS("Gtk2::CellView::get_size_of_row", XS_Gtk2__CellView_get_size_of_row, file);
        newXS("Gtk2::CellView::set_background_color", XS_Gtk2__CellView_set_background_color, file);
        newXS("Gtk2::CellView::get_cell_renderers", XS_Gtk2__CellView_get_cell_renderers, file);
#if XSubPPtmpAAAA
        newXS("Gtk2::CellView::get_model", XS_Gtk2__CellView_get_model, file);
#endif

    /* Initialisation Section */

#if XSubPPtmpAAAA
#endif
#line 440 "xs\\GtkCellView.c"

    /* End of Initialisation Section */

#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

