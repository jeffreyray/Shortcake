/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.16 from the
 * contents of GtkEntryBuffer.xs. Do not edit this file, edit GtkEntryBuffer.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "xs\\\\GtkEntryBuffer.xs"
/*
 * Copyright (c) 2010 by the gtk2-perl team (see the file AUTHORS)
 *
 * This library is free software; you can redistribute it and/or
 * modify it under the terms of the GNU Library General Public
 * License as published by the Free Software Foundation; either
 * version 2.1 of the License, or (at your option) any later version.
 *
 * This library is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the GNU
 * Library General Public License for more details.
 *
 * You should have received a copy of the GNU Library General Public
 * License along with this library. If not, see http://www.gnu.org/licenses/
 */

#include "gtk2perl.h"

#line 30 "xs\\GtkEntryBuffer.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 172 "xs\\GtkEntryBuffer.c"

XS_EUPXS(XS_Gtk2__EntryBuffer_new); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_new)
{
    dVAR; dXSARGS;
    if (items < 1 || items > 2)
       croak_xs_usage(cv,  "class, initial_chars=NULL");
    {
	GtkEntryBuffer_noinc *	RETVAL;
	STRLEN	STRLEN_length_of_initial_chars;
	gint	XSauto_length_of_initial_chars;
	const gchar_utf8_length *	initial_chars;

	XSauto_length_of_initial_chars = STRLEN_length_of_initial_chars;

	if (items < 2)
	    initial_chars = NULL;
	else {
	    sv_utf8_upgrade (ST(1));
	    initial_chars = (const gchar_utf8_length *)SvPV (ST(1), STRLEN_length_of_initial_chars);
	XSauto_length_of_initial_chars = g_utf8_strlen (initial_chars, STRLEN_length_of_initial_chars)
;
	}
#line 46 "xs\\\\GtkEntryBuffer.xs"
        if (initial_chars == NULL) {
                RETVAL = gtk_entry_buffer_new (NULL, 0);
        }
        else {
                RETVAL = gtk_entry_buffer_new (initial_chars, XSauto_length_of_initial_chars);
        }
#line 203 "xs\\GtkEntryBuffer.c"
	ST(0) = newSVGtkEntryBuffer_noinc (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_get_bytes); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_get_bytes)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "buffer");
    {
	gsize	RETVAL;
	dXSTARG;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;

	RETVAL = gtk_entry_buffer_get_bytes(buffer);
	XSprePUSH; PUSHu((UV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_get_length); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_get_length)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "buffer");
    {
	guint	RETVAL;
	dXSTARG;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;

	RETVAL = gtk_entry_buffer_get_length(buffer);
	XSprePUSH; PUSHu((UV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_get_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_get_text)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "buffer");
    {
	const gchar *	RETVAL;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;

	RETVAL = gtk_entry_buffer_get_text(buffer);
	ST(0) = sv_newmortal();
	/* same as newSVGChar(), but not in a function */
	sv_setpv ((SV*)ST(0), RETVAL);
	SvUTF8_on (ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_set_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_set_text)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "buffer, chars");
    {
	STRLEN	STRLEN_length_of_chars;
	gint	XSauto_length_of_chars;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	const gchar_utf8_length *	chars;

	XSauto_length_of_chars = STRLEN_length_of_chars;

	sv_utf8_upgrade (ST(1));
	chars = (const gchar_utf8_length *)SvPV (ST(1), STRLEN_length_of_chars);
        XSauto_length_of_chars = g_utf8_strlen (chars, STRLEN_length_of_chars)
;

	gtk_entry_buffer_set_text(buffer, chars, XSauto_length_of_chars);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__EntryBuffer_set_max_length); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_set_max_length)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "buffer, max_length");
    {
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	gint	max_length = (gint)SvIV(ST(1))
;

	gtk_entry_buffer_set_max_length(buffer, max_length);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__EntryBuffer_get_max_length); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_get_max_length)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "buffer");
    {
	gint	RETVAL;
	dXSTARG;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;

	RETVAL = gtk_entry_buffer_get_max_length(buffer);
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_insert_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_insert_text)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "buffer, position, chars");
    {
	STRLEN	STRLEN_length_of_chars;
	gint	XSauto_length_of_chars;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	guint	position = (guint)SvUV(ST(1))
;
	const gchar_utf8_length *	chars;

	XSauto_length_of_chars = STRLEN_length_of_chars;

	sv_utf8_upgrade (ST(2));
	chars = (const gchar_utf8_length *)SvPV (ST(2), STRLEN_length_of_chars);
        XSauto_length_of_chars = g_utf8_strlen (chars, STRLEN_length_of_chars)
;

	gtk_entry_buffer_insert_text(buffer, position, chars, XSauto_length_of_chars);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__EntryBuffer_delete_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_delete_text)
{
    dVAR; dXSARGS;
    if (items < 1 || items > 3)
       croak_xs_usage(cv,  "buffer, position=0, n_chars=-1");
    {
	guint	RETVAL;
	dXSTARG;
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	guint	position;
	gint	n_chars;

	if (items < 2)
	    position = 0;
	else {
	    position = (guint)SvUV(ST(1))
;
	}

	if (items < 3)
	    n_chars = -1;
	else {
	    n_chars = (gint)SvIV(ST(2))
;
	}

	RETVAL = gtk_entry_buffer_delete_text(buffer, position, n_chars);
	XSprePUSH; PUSHu((UV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__EntryBuffer_emit_inserted_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_emit_inserted_text)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "buffer, position, chars, n_chars");
    {
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	guint	position = (guint)SvUV(ST(1))
;
	const gchar *	chars;
	guint	n_chars = (guint)SvUV(ST(3))
;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(2));
	chars = (const gchar *)SvPV_nolen (ST(2))
;

	gtk_entry_buffer_emit_inserted_text(buffer, position, chars, n_chars);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__EntryBuffer_emit_deleted_text); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__EntryBuffer_emit_deleted_text)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "buffer, position, n_chars");
    {
	GtkEntryBuffer *	buffer = SvGtkEntryBuffer (ST(0))
;
	guint	position = (guint)SvUV(ST(1))
;
	guint	n_chars = (guint)SvUV(ST(2))
;

	gtk_entry_buffer_emit_deleted_text(buffer, position, n_chars);
    }
    XSRETURN_EMPTY;
}

#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Gtk2__EntryBuffer); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Gtk2__EntryBuffer)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        newXS("Gtk2::EntryBuffer::new", XS_Gtk2__EntryBuffer_new, file);
        newXS("Gtk2::EntryBuffer::get_bytes", XS_Gtk2__EntryBuffer_get_bytes, file);
        newXS("Gtk2::EntryBuffer::get_length", XS_Gtk2__EntryBuffer_get_length, file);
        newXS("Gtk2::EntryBuffer::get_text", XS_Gtk2__EntryBuffer_get_text, file);
        newXS("Gtk2::EntryBuffer::set_text", XS_Gtk2__EntryBuffer_set_text, file);
        newXS("Gtk2::EntryBuffer::set_max_length", XS_Gtk2__EntryBuffer_set_max_length, file);
        newXS("Gtk2::EntryBuffer::get_max_length", XS_Gtk2__EntryBuffer_get_max_length, file);
        newXS("Gtk2::EntryBuffer::insert_text", XS_Gtk2__EntryBuffer_insert_text, file);
        newXS("Gtk2::EntryBuffer::delete_text", XS_Gtk2__EntryBuffer_delete_text, file);
        newXS("Gtk2::EntryBuffer::emit_inserted_text", XS_Gtk2__EntryBuffer_emit_inserted_text, file);
        newXS("Gtk2::EntryBuffer::emit_deleted_text", XS_Gtk2__EntryBuffer_emit_deleted_text, file);
#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

