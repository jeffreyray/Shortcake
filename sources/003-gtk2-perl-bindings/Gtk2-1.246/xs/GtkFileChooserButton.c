/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.16 from the
 * contents of GtkFileChooserButton.xs. Do not edit this file, edit GtkFileChooserButton.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "xs\\\\GtkFileChooserButton.xs"
/*
 * Copyright (c) 2004-2006 by the gtk2-perl team (see the file AUTHORS)
 *
 * Licensed under the LGPL, see LICENSE file for more information.
 *
 * $Id$
 */

#include "gtk2perl.h"

#line 21 "xs\\GtkFileChooserButton.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 163 "xs\\GtkFileChooserButton.c"

XS_EUPXS(XS_Gtk2__FileChooserButton_new); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_new)
{
    dVAR; dXSARGS;
    if (items != 3)
       croak_xs_usage(cv,  "class, title, action");
    {
	const gchar *	title;
	GtkFileChooserAction	action = SvGtkFileChooserAction (ST(2))
;
	GtkWidget *	RETVAL;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	title = (const gchar *)SvPV_nolen (ST(1))
;

	RETVAL = gtk_file_chooser_button_new(title, action);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__FileChooserButton_new_with_backend); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_new_with_backend)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "class, title, action, backend");
    {
	const gchar *	title;
	GtkFileChooserAction	action = SvGtkFileChooserAction (ST(2))
;
	const gchar *	backend;
	GtkWidget *	RETVAL;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	title = (const gchar *)SvPV_nolen (ST(1))
;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(3));
	backend = (const gchar *)SvPV_nolen (ST(3))
;

	RETVAL = gtk_file_chooser_button_new_with_backend(title, action, backend);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__FileChooserButton_new_with_dialog); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_new_with_dialog)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, dialog");
    {
	GtkWidget *	dialog = SvGtkWidget (ST(1))
;
	GtkWidget *	RETVAL;

	RETVAL = gtk_file_chooser_button_new_with_dialog(dialog);
	ST(0) = newSVGtkWidget (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__FileChooserButton_get_title); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_get_title)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "button");
    {
	const gchar *	RETVAL;
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;

	RETVAL = gtk_file_chooser_button_get_title(button);
	ST(0) = sv_newmortal();
	/* same as newSVGChar(), but not in a function */
	sv_setpv ((SV*)ST(0), RETVAL);
	SvUTF8_on (ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__FileChooserButton_set_title); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_set_title)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "button, title");
    {
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;
	const gchar *	title;

	/* same as SvGChar(), but not in a function */
	sv_utf8_upgrade (ST(1));
	title = (const gchar *)SvPV_nolen (ST(1))
;

	gtk_file_chooser_button_set_title(button, title);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__FileChooserButton_get_width_chars); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_get_width_chars)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "button");
    {
	gint	RETVAL;
	dXSTARG;
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;

	RETVAL = gtk_file_chooser_button_get_width_chars(button);
	XSprePUSH; PUSHi((IV)RETVAL);
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__FileChooserButton_set_width_chars); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_set_width_chars)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "button, n_chars");
    {
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;
	gint	n_chars = (gint)SvIV(ST(1))
;

	gtk_file_chooser_button_set_width_chars(button, n_chars);
    }
    XSRETURN_EMPTY;
}

#if GTK_CHECK_VERSION (2, 10, 0)
#define XSubPPtmpAAAA 1


XS_EUPXS(XS_Gtk2__FileChooserButton_set_focus_on_click); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_set_focus_on_click)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "button, focus_on_click");
    {
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;
	gboolean	focus_on_click = (bool)SvTRUE(ST(1))
;

	gtk_file_chooser_button_set_focus_on_click(button, focus_on_click);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__FileChooserButton_get_focus_on_click); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__FileChooserButton_get_focus_on_click)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "button");
    {
	gboolean	RETVAL;
	GtkFileChooserButton *	button = SvGtkFileChooserButton (ST(0))
;

	RETVAL = gtk_file_chooser_button_get_focus_on_click(button);
	ST(0) = boolSV(RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}

#endif
#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Gtk2__FileChooserButton); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Gtk2__FileChooserButton)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        newXS("Gtk2::FileChooserButton::new", XS_Gtk2__FileChooserButton_new, file);
        newXS("Gtk2::FileChooserButton::new_with_backend", XS_Gtk2__FileChooserButton_new_with_backend, file);
        newXS("Gtk2::FileChooserButton::new_with_dialog", XS_Gtk2__FileChooserButton_new_with_dialog, file);
        newXS("Gtk2::FileChooserButton::get_title", XS_Gtk2__FileChooserButton_get_title, file);
        newXS("Gtk2::FileChooserButton::set_title", XS_Gtk2__FileChooserButton_set_title, file);
        newXS("Gtk2::FileChooserButton::get_width_chars", XS_Gtk2__FileChooserButton_get_width_chars, file);
        newXS("Gtk2::FileChooserButton::set_width_chars", XS_Gtk2__FileChooserButton_set_width_chars, file);
#if XSubPPtmpAAAA
        newXS("Gtk2::FileChooserButton::set_focus_on_click", XS_Gtk2__FileChooserButton_set_focus_on_click, file);
        newXS("Gtk2::FileChooserButton::get_focus_on_click", XS_Gtk2__FileChooserButton_get_focus_on_click, file);
#endif

    /* Initialisation Section */

#line 14 "xs\\\\GtkFileChooserButton.xs"
	gperl_prepend_isa ("Gtk2::FileChooserButton", "Gtk2::FileChooser");

#if XSubPPtmpAAAA
#endif
#line 399 "xs\\GtkFileChooserButton.c"

    /* End of Initialisation Section */

#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

