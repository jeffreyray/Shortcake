/*
 * This file was generated automatically by ExtUtils::ParseXS version 3.16 from the
 * contents of GdkCairo.xs. Do not edit this file, edit GdkCairo.xs instead.
 *
 *    ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "xs\\\\GdkCairo.xs"
/*
 * Copyright (c) 2005, 2011 by the gtk2-perl team (see the file AUTHORS)
 *
 * Licensed under the LGPL, see LICENSE file for more information.
 *
 * $Id$
 */

#include "gtk2perl.h"
#include <cairo-perl.h>

#line 22 "xs\\GdkCairo.c"
#ifndef PERL_UNUSED_VAR
#  define PERL_UNUSED_VAR(var) if (0) var = var
#endif

#ifndef dVAR
#  define dVAR		dNOOP
#endif


/* This stuff is not part of the API! You have been warned. */
#ifndef PERL_VERSION_DECIMAL
#  define PERL_VERSION_DECIMAL(r,v,s) (r*1000000 + v*1000 + s)
#endif
#ifndef PERL_DECIMAL_VERSION
#  define PERL_DECIMAL_VERSION \
	  PERL_VERSION_DECIMAL(PERL_REVISION,PERL_VERSION,PERL_SUBVERSION)
#endif
#ifndef PERL_VERSION_GE
#  define PERL_VERSION_GE(r,v,s) \
	  (PERL_DECIMAL_VERSION >= PERL_VERSION_DECIMAL(r,v,s))
#endif
#ifndef PERL_VERSION_LE
#  define PERL_VERSION_LE(r,v,s) \
	  (PERL_DECIMAL_VERSION <= PERL_VERSION_DECIMAL(r,v,s))
#endif

/* XS_INTERNAL is the explicit static-linkage variant of the default
 * XS macro.
 *
 * XS_EXTERNAL is the same as XS_INTERNAL except it does not include
 * "STATIC", ie. it exports XSUB symbols. You probably don't want that
 * for anything but the BOOT XSUB.
 *
 * See XSUB.h in core!
 */


/* TODO: This might be compatible further back than 5.10.0. */
#if PERL_VERSION_GE(5, 10, 0) && PERL_VERSION_LE(5, 15, 1)
#  undef XS_EXTERNAL
#  undef XS_INTERNAL
#  if defined(__CYGWIN__) && defined(USE_DYNAMIC_LOADING)
#    define XS_EXTERNAL(name) __declspec(dllexport) XSPROTO(name)
#    define XS_INTERNAL(name) STATIC XSPROTO(name)
#  endif
#  if defined(__SYMBIAN32__)
#    define XS_EXTERNAL(name) EXPORT_C XSPROTO(name)
#    define XS_INTERNAL(name) EXPORT_C STATIC XSPROTO(name)
#  endif
#  ifndef XS_EXTERNAL
#    if defined(HASATTRIBUTE_UNUSED) && !defined(__cplusplus)
#      define XS_EXTERNAL(name) void name(pTHX_ CV* cv __attribute__unused__)
#      define XS_INTERNAL(name) STATIC void name(pTHX_ CV* cv __attribute__unused__)
#    else
#      ifdef __cplusplus
#        define XS_EXTERNAL(name) extern "C" XSPROTO(name)
#        define XS_INTERNAL(name) static XSPROTO(name)
#      else
#        define XS_EXTERNAL(name) XSPROTO(name)
#        define XS_INTERNAL(name) STATIC XSPROTO(name)
#      endif
#    endif
#  endif
#endif

/* perl >= 5.10.0 && perl <= 5.15.1 */


/* The XS_EXTERNAL macro is used for functions that must not be static
 * like the boot XSUB of a module. If perl didn't have an XS_EXTERNAL
 * macro defined, the best we can do is assume XS is the same.
 * Dito for XS_INTERNAL.
 */
#ifndef XS_EXTERNAL
#  define XS_EXTERNAL(name) XS(name)
#endif
#ifndef XS_INTERNAL
#  define XS_INTERNAL(name) XS(name)
#endif

/* Now, finally, after all this mess, we want an ExtUtils::ParseXS
 * internal macro that we're free to redefine for varying linkage due
 * to the EXPORT_XSUB_SYMBOLS XS keyword. This is internal, use
 * XS_EXTERNAL(name) or XS_INTERNAL(name) in your code if you need to!
 */

#undef XS_EUPXS
#if defined(PERL_EUPXS_ALWAYS_EXPORT)
#  define XS_EUPXS(name) XS_EXTERNAL(name)
#else
   /* default to internal */
#  define XS_EUPXS(name) XS_INTERNAL(name)
#endif

#ifndef PERL_ARGS_ASSERT_CROAK_XS_USAGE
#define PERL_ARGS_ASSERT_CROAK_XS_USAGE assert(cv); assert(params)

/* prototype to pass -Wmissing-prototypes */
STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params);

STATIC void
S_croak_xs_usage(pTHX_ const CV *const cv, const char *const params)
{
    const GV *const gv = CvGV(cv);

    PERL_ARGS_ASSERT_CROAK_XS_USAGE;

    if (gv) {
        const char *const gvname = GvNAME(gv);
        const HV *const stash = GvSTASH(gv);
        const char *const hvname = stash ? HvNAME(stash) : NULL;

        if (hvname)
            Perl_croak(aTHX_ "Usage: %s::%s(%s)", hvname, gvname, params);
        else
            Perl_croak(aTHX_ "Usage: %s(%s)", gvname, params);
    } else {
        /* Pants. I don't think that it should be possible to get here. */
        Perl_croak(aTHX_ "Usage: CODE(0x%"UVxf")(%s)", PTR2UV(cv), params);
    }
}
#undef  PERL_ARGS_ASSERT_CROAK_XS_USAGE

#ifdef PERL_IMPLICIT_CONTEXT
#define croak_xs_usage(a,b)    S_croak_xs_usage(aTHX_ a,b)
#else
#define croak_xs_usage        S_croak_xs_usage
#endif

#endif

/* NOTE: the prototype of newXSproto() is different in versions of perls,
 * so we define a portable version of newXSproto()
 */
#ifdef newXS_flags
#define newXSproto_portable(name, c_impl, file, proto) newXS_flags(name, c_impl, file, proto, 0)
#else
#define newXSproto_portable(name, c_impl, file, proto) (PL_Sv=(SV*)newXS(name, c_impl, file), sv_setpv(PL_Sv, proto), (CV*)PL_Sv)
#endif /* !defined(newXS_flags) */

#line 164 "xs\\GdkCairo.c"

XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_create); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_create)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "class, drawable");
    {
#line 49 "xs\\\\GdkCairo.xs"
	cairo_t *cr;
#line 175 "xs\\GdkCairo.c"
	SV *	RETVAL;
	GdkDrawable *	drawable = SvGdkDrawable (ST(1))
;
#line 51 "xs\\\\GdkCairo.xs"
	/* We own cr. */
	cr = gdk_cairo_create (drawable);
	RETVAL = newSV (0);
	sv_setref_pv (RETVAL, "Gtk2::Gdk::Cairo::Context", (void *) cr);
#line 184 "xs\\GdkCairo.c"
	ST(0) = RETVAL;
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_color); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_color)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cr, color");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
	GdkColor *	color = SvGdkColor (ST(1))
;

	gdk_cairo_set_source_color(cr, color);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_pixbuf); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_pixbuf)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "cr, pixbuf, pixbuf_x, pixbuf_y");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
	GdkPixbuf *	pixbuf = SvGdkPixbuf (ST(1))
;
	double	pixbuf_x = (double)SvNV(ST(2))
;
	double	pixbuf_y = (double)SvNV(ST(3))
;

	gdk_cairo_set_source_pixbuf(cr, pixbuf, pixbuf_x, pixbuf_y);
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_rectangle); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_rectangle)
{
    dVAR; dXSARGS;
    if (items < 1)
       croak_xs_usage(cv,  "cr, ...");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
#line 82 "xs\\\\GdkCairo.xs"
	if (items == 2) {
		GdkRectangle *rect = SvGdkRectangle (ST (1));
		gdk_cairo_rectangle (cr, rect);
	} else if (items == 5) {
		double x = SvNV (ST(1));
		double y = SvNV (ST(2));
		double width = SvNV (ST(3));
		double height = SvNV (ST(4));
		cairo_rectangle (cr, x, y, width, height);
	} else {
		croak ("Usage: Gtk2::Gdk::Cairo::Context::rectangle (cr, rectangle)");
	}
#line 254 "xs\\GdkCairo.c"
    }
    XSRETURN_EMPTY;
}


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_region); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_region)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cr, region");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
	GdkRegion *	region = SvGdkRegion (ST(1))
;

	gdk_cairo_region(cr, region);
    }
    XSRETURN_EMPTY;
}

#if GTK_CHECK_VERSION (2, 10, 0)
#define XSubPPtmpAAAA 1


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_pixmap); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_set_source_pixmap)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "cr, pixmap, pixmap_x, pixmap_y");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
	GdkPixmap *	pixmap = SvGdkPixmap (ST(1))
;
	double	pixmap_x = (double)SvNV(ST(2))
;
	double	pixmap_y = (double)SvNV(ST(3))
;

	gdk_cairo_set_source_pixmap(cr, pixmap, pixmap_x, pixmap_y);
    }
    XSRETURN_EMPTY;
}

#endif
#if GTK_CHECK_VERSION (2, 18, 0)
#define XSubPPtmpAAAB 1


XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_reset_clip); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Cairo__Context_reset_clip)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "cr, drawable");
    {
	cairo_t *	cr = SvCairo (ST(0))
;
	GdkDrawable *	drawable = SvGdkDrawable (ST(1))
;

	gdk_cairo_reset_clip(cr, drawable);
    }
    XSRETURN_EMPTY;
}

#endif
#if GTK_CHECK_VERSION (2, 10, 0)
#define XSubPPtmpAAAC 1


XS_EUPXS(XS_Gtk2__Gdk__Screen_get_font_options); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Screen_get_font_options)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "screen");
    {
	const cairo_font_options_t_ornull *	RETVAL;
	GdkScreen *	screen = SvGdkScreen (ST(0))
;

	RETVAL = gdk_screen_get_font_options(screen);
	ST(0) = newSVCairoFontOptions_ornull (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__Gdk__Screen_set_font_options); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Screen_set_font_options)
{
    dVAR; dXSARGS;
    if (items != 2)
       croak_xs_usage(cv,  "screen, options");
    {
	GdkScreen *	screen = SvGdkScreen (ST(0))
;
	const cairo_font_options_t_ornull *	options = SvCairoFontOptions_ornull (ST(1))
;

	gdk_screen_set_font_options(screen, options);
    }
    XSRETURN_EMPTY;
}

#endif
#if GTK_CHECK_VERSION (2, 22, 0)
#define XSubPPtmpAAAD 1


XS_EUPXS(XS_Gtk2__Gdk__Window_create_similar_surface); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Window_create_similar_surface)
{
    dVAR; dXSARGS;
    if (items != 4)
       croak_xs_usage(cv,  "window, content, width, height");
    {
	cairo_surface_t *	RETVAL;
	GdkWindow *	window = SvGdkWindow (ST(0))
;
	cairo_content_t	content = SvCairoContent (ST(1))
;
	int	width = (int)SvIV(ST(2))
;
	int	height = (int)SvIV(ST(3))
;

	RETVAL = gdk_window_create_similar_surface(window, content, width, height);
	ST(0) = newSVCairoSurface (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}


XS_EUPXS(XS_Gtk2__Gdk__Window_get_background_pattern); /* prototype to pass -Wmissing-prototypes */
XS_EUPXS(XS_Gtk2__Gdk__Window_get_background_pattern)
{
    dVAR; dXSARGS;
    if (items != 1)
       croak_xs_usage(cv,  "window");
    {
	cairo_pattern_t *	RETVAL;
	GdkWindow *	window = SvGdkWindow (ST(0))
;

	RETVAL = gdk_window_get_background_pattern(window);
	ST(0) = newSVCairoPattern (RETVAL);
	sv_2mortal(ST(0));
    }
    XSRETURN(1);
}

#endif
#ifdef __cplusplus
extern "C"
#endif
XS_EXTERNAL(boot_Gtk2__Gdk__Cairo); /* prototype to pass -Wmissing-prototypes */
XS_EXTERNAL(boot_Gtk2__Gdk__Cairo)
{
    dVAR; dXSARGS;
#if (PERL_REVISION == 5 && PERL_VERSION < 9)
    char* file = __FILE__;
#else
    const char* file = __FILE__;
#endif

    PERL_UNUSED_VAR(cv); /* -W */
    PERL_UNUSED_VAR(items); /* -W */
#ifdef XS_APIVERSION_BOOTCHECK
    XS_APIVERSION_BOOTCHECK;
#endif
    XS_VERSION_BOOTCHECK;

        newXS("Gtk2::Gdk::Cairo::Context::create", XS_Gtk2__Gdk__Cairo__Context_create, file);
        newXS("Gtk2::Gdk::Cairo::Context::set_source_color", XS_Gtk2__Gdk__Cairo__Context_set_source_color, file);
        newXS("Gtk2::Gdk::Cairo::Context::set_source_pixbuf", XS_Gtk2__Gdk__Cairo__Context_set_source_pixbuf, file);
        newXS("Gtk2::Gdk::Cairo::Context::rectangle", XS_Gtk2__Gdk__Cairo__Context_rectangle, file);
        newXS("Gtk2::Gdk::Cairo::Context::region", XS_Gtk2__Gdk__Cairo__Context_region, file);
#if XSubPPtmpAAAA
        newXS("Gtk2::Gdk::Cairo::Context::set_source_pixmap", XS_Gtk2__Gdk__Cairo__Context_set_source_pixmap, file);
#endif
#if XSubPPtmpAAAB
        newXS("Gtk2::Gdk::Cairo::Context::reset_clip", XS_Gtk2__Gdk__Cairo__Context_reset_clip, file);
#endif
#if XSubPPtmpAAAC
        newXS("Gtk2::Gdk::Screen::get_font_options", XS_Gtk2__Gdk__Screen_get_font_options, file);
        newXS("Gtk2::Gdk::Screen::set_font_options", XS_Gtk2__Gdk__Screen_set_font_options, file);
#endif
#if XSubPPtmpAAAD
        newXS("Gtk2::Gdk::Window::create_similar_surface", XS_Gtk2__Gdk__Window_create_similar_surface, file);
        newXS("Gtk2::Gdk::Window::get_background_pattern", XS_Gtk2__Gdk__Window_get_background_pattern, file);
#endif

    /* Initialisation Section */

#line 41 "xs\\\\GdkCairo.xs"
	gperl_set_isa ("Gtk2::Gdk::Cairo::Context", "Cairo::Context");

#if XSubPPtmpAAAA
#endif
#if XSubPPtmpAAAB
#endif
#if XSubPPtmpAAAC
#endif
#if XSubPPtmpAAAD
#endif
#line 467 "xs\\GdkCairo.c"

    /* End of Initialisation Section */

#if (PERL_REVISION == 5 && PERL_VERSION >= 9)
  if (PL_unitcheckav)
       call_list(PL_scopestack_ix, PL_unitcheckav);
#endif
    XSRETURN_YES;
}

